'use strict';

const log4js = require('log4js');
const adp = require('../helpers/adapter');
const db = require('../helpers/db');
const log = log4js.getLogger('ISE-NODES');

class GlobISE {
}

module.exports = {
    GlobISE: GlobISE,
}